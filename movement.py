#This is the movement when the player is in red heart mode


square = pygame.Rect(
    (800 - square_size) // 2,
    (600 - square_size) // 2,
    square_size,
    square_size,
)
square_x = float(square.x)
square_y = float(square.y)








































#This is the movement when the player is in blue heart mode
